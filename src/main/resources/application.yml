server:
  port: 8083

spring:
  application:
    name: sticker-service
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://34.31.54.248:3306/glue_sticker
    username: root
    password: glue2024!
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
        dialect: org.hibernate.dialect.MySQLDialect
    logging.level:
      org.hibernate.SQL: debug

springdoc:
  api-docs:
    version: openapi_3_1
    enabled: true
    path: /blogs/v3/api-docs
  enable-spring-security: true
  default-consumes-media-type: application/json
  default-produces-media-type: application/json